spring.application.name=qualitrack
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

#ADMIN CREDENTIALS
app.admin.email=${ADMIN_EMAIL}
app.admin.password=${ADMIN_PASSWORD}

#FILE PATH CREDENTIALS
